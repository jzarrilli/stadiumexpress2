<!DOCTYPE html>
<html> <head>
<!--<title>Stadium Express</title> -->
<%= stylesheet_link_tag "scaffold" %> <%= stylesheet_link_tag "depot", :media => "all" %>
 <%= javascript_include_tag :defaults %> <%= csrf_meta_tag %>
</head> 

<body	id="store" >
	<div id="banner"> 
		<%= link_to image_tag("logo2.jpg", :size => "400x200", :border =>0), root_path %>
	<!--	<%= @page_title || "      Stadium Express" %>-->
		</div> 
		
		<div id="columns">
		
			<% if !session[:user_id] %>		
		
				<div id="stripe">
					<%= link_to "Vendor Login", login_path%> <br/>
				</div>
			
				<div id="main">
					<%= yield %>
				</div>
			<% end %>
		
		
			<% if session[:user_id] %>
				<div id="side2"> 
			
					<%= link_to "User Home", root_path%> <br/>

					<%= link_to 'Orders',	orders_path	%><br />
					<%= link_to 'Products', products_path %><br />
					<%= link_to 'Users',	users_path	%><br />
					<%= button_to 'Logout', logout_path, :method => :delete	%>
				</div> 
		
				<div id="main2">
					<%= yield %>
				</div> 	
			<% end %>		
	</div>

	
</body> 
</html>
			
